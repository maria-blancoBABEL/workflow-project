name: 04 - Uso de Actions

on:
  push:  # Se ejecuta automáticamente con cada push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Printing Folders
        run: ls -R


